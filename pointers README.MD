1) what is pointer?
 
A pointer is a user defined data type which is used to store the address of variable 

2) what are the types of pointers?
types of pointers 
 1.NULL pointer 
 2.void pointer 
 3.wild pointer 
 4.darling pointer 

3) what is null pointer?

Null pointer points nothing .it indicates that the pointer is not pointing to any memory address.

4) what is wild pointer?

A pointer which is declared  but not initialized.

5) what is double pointer?

A pointer which stores the address of a pointer not a normal variable.

6) what is void pointer?
 void pointer is a generic pointer , which stores the address of any data type.

7) what is * and &?

dereference operator which is used to return the value
address operator which is used to return the address .

8) what is the difference between p++ and (*p)++?

p++ is used to  moves to next memory location,address is changed.
(*p)++ is used to increment the value pointeb to by the pointer.

9) how do pointers work with dynamic memory allocation(malloc,calloc,realloc,free)?

pointers are essential for dynamic memory allocation because functions like malloc(),calloc(),realloc() return address of the memory .the pointer stores this adresss so we can access the allocated memory.

10) can a pointer points to another pointer?

Yes, a pointer  can point to another pointer this pointer is called as double pointer. 

